{
	"name": "inngestgo",
	"private": true,
	"version": "0.11.2",
	"scripts": {
		"changeset": "changeset",
		"release:version": "changeset version && ./set_version.sh && mv CHANGELOG.md ../",
		"release:publish": "changeset tag"
	},
	"dependencies": {
		"@changesets/cli": "^2.28.1"
	}
}
